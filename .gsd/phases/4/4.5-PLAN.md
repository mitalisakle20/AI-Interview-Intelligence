---
phase: 4
plan: 5
wave: 3
---

# Plan 4.5: Interview Guide Merge & PDF Generation

## Objective
Combine the interviewee's feedback into the Interviewer's Guide and implement the client-to-server PDF generation flow.

## Context
- frontend/src/pages/InterviewerDashboard.jsx
- frontend/src/services/api.js

## Tasks

<task type="auto">
  <name>Implement Feedback Merge in Interviewer Guide</name>
  <files>
    - frontend/src/components/BriefView.jsx
  </files>
  <action>
    - Modify the `BriefView.jsx` component.
    - Check if `session.feedback` exists. If so, render a high-visibility "Interviewee Feedback received" banner near the top (e.g., Maroon or Gold background).
    - Display the text from `session.feedback.corrections` under an "Important Corrections" header.
    - Display the `session.feedback.selectedQuestions` prominently before the rest of the generated questions.
  </action>
  <verify>cat frontend/src/components/BriefView.jsx</verify>
  <done>BriefView intelligently surfaces interviewee feedback when it is available in the session.</done>
</task>

<task type="auto">
  <name>Implement PDF Generation Button</name>
  <files>
    - frontend/src/components/PdfDownloadButton.jsx
  </files>
  <action>
    - Create a React component containing a "Download PDF" button.
    - Accept a `targetId` or HTML string as a prop.
    - On click, gather the HTML, append basic Tailwind `<style>` classes if necessary, and call `api.generatePdf(htmlString)`.
    - Take the `base64` response, convert it to a Blob (`application/pdf`), and trigger a browser download via an invisible `<a>` tag.
    - Add this button to the top of both the `InterviewerDashboard` and the `IntervieweePortal`.
  </action>
  <verify>cd frontend && npm run build</verify>
  <done>Button successfully triggers the API and downloads a PDF file.</done>
</task>

## Success Criteria
- [ ] Interviewer UI updates cleanly to show corrections.
- [ ] PDF download feature works globally.
