---
phase: 4
plan: 2
wave: 1
---

# Plan 4.2: React Setup & Core Services

## Objective
Establish the frontend foundational structure, routing, and Texas A&M styling theme.

## Context
- .gsd/SPEC.md
- frontend/package.json
- frontend/tailwind.config.js (or create if missing)

## Tasks

<task type="auto">
  <name>Configure Tailwind CSS</name>
  <files>
    - frontend/tailwind.config.js
    - frontend/index.html
    - frontend/src/index.css
  </files>
  <action>
    - Initialize Tailwind CSS if not already configured (`npx tailwindcss init -p` within `frontend/` if needed, but it should be standard Vite+React setup).
    - Update `tailwind.config.js` to include the Texas A&M Maroon theme color: `theme: { extend: { colors: { maroon: { 600: '#500000', 700: '#3c0000' } } } }`.
    - Ensure `@tailwind base; @tailwind components; @tailwind utilities;` is in `src/index.css`.
  </action>
  <verify>cd frontend && npm run build</verify>
  <done>Tailwind compiles successfully with custom maroon colors.</done>
</task>

<task type="auto">
  <name>Create API Service Client</name>
  <files>
    - frontend/src/services/api.js
  </files>
  <action>
    - Create a wrapper for fetch calls pointing to the API Gateway URL (from backend outputs or `.env`).
    - Implement `triggerPipeline(companyName, companyUrl)`.
    - Implement `getSession(sessionId)`.
    - Implement `submitFeedback(sessionId, corrections, selectedQuestions)`.
    - Implement `generatePdf(htmlContent)`.
  </action>
  <verify>cat frontend/src/services/api.js</verify>
  <done>api.js exports functions for the 4 core backend endpoints.</done>
</task>

<task type="auto">
  <name>Set Up React Router</name>
  <files>
    - frontend/src/App.jsx
    - frontend/src/main.jsx
  </files>
  <action>
    - Configure `react-router-dom` with three main routes:
      1. `/` -> Interviewer Setup / Trigger
      2. `/session/:id` -> Interviewer Dashboard
      3. `/interviewee/:id` -> Interviewee Portal
    - Create placeholder components for each route to ensure navigation works.
  </action>
  <verify>cd frontend && npm run build</verify>
  <done>App builds with router configured and wrapper layout in place.</done>
</task>

## Success Criteria
- [ ] Tailwind CSS applies the `#500000` color.
- [ ] API service module exists.
- [ ] 3 main application routes are structured.
